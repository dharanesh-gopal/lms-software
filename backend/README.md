# LMS Backend

Simple Express + Mongoose backend for development.

Scripts:

- `npm run dev` — start with `nodemon` (requires dev deps)
- `npm start` — start once

API endpoints:

- `GET /api/health` — health check
- `GET /api/users` — list users
- `POST /api/users` — create user `{ name, email }`
